# Python Project

This project is built with Python and includes complete instructions for setting up the development environment, installing dependencies, and running the application.  
Follow the steps below to ensure a correct and reproducible setup.

---

## 🚀 Environment Setup

### 1️⃣ Check Python Version

This project requires **Python 3.10+**.

```bash
python3 --version
```

### Run the following command inside the project root directory:
``` bash 
python3 -m venv venv
```
### activate env
```bash
#macOS / Linux:

source venv/bin/activate


#Windows (PowerShell):

pip install --upgrade pip setuptools wheel

#You should see (venv) appear before your terminal prompt.
```

### Upgrade pip (Recommended)
```bash 
pip install --upgrade pip setuptools wheel

```

### Installing Dependencies

```bash 
pip install -r requirements.txt
```

### Running the Project

```bash
python main.py

```